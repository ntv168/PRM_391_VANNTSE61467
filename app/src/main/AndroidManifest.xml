<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sam.prm_391">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:icon="@drawable/ic_launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity
            android:name=".Activities_Intent.IntentDemo"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".Activities_Intent.ChildActivity" />
        <activity android:name=".Activities_Intent.ResultActivity" />
        <activity android:name=".Activities_Intent.SendSMSActivity" />
        <activity android:name=".ActionBar.ActionBarMain" />
        <activity android:name=".View_Flipper"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".SdlingDrawer.Sliding_drawer" />
        <activity android:name=".SdlingDrawer.ActivityHorizontal" />
        <activity
            android:name=".Notification.NotificationMain"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".Notification.NotificationSecond"></activity>
    </application>

</manifest>